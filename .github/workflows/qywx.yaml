name: test

on: [push]
jobs:
  qywx:
    runs-on: ubuntu-latest
    steps:
      # 企业微信通知
      - id: wx_prep
        run: |
          echo ::set-output name=wx_created::$(date -u +'%Y-%m-%d %H:%M:%SZ')
          echo ::set-output name=wx_base_ref::echo ${github.base_ref#refs/heads/}
          echo ::set-output name=wx_target_ref::echo ${github.event.ref#refs/heads/}
      - name: WeChat Work Notification
        uses: chf007/action-wechat-work@master
        env:
          WECHAT_WORK_BOT_WEBHOOK: ${{secrets.WECHAT_WORK_BOT_WEBHOOK}}
        with:
          msgtype: markdown
          content: "🎉**【[${{ github.repository }}](https://github.com/${{ github.repository }})】** \n
          \n
          🏃 <font color=\"info\">${{ github.actor }}</font>\n
          🕐 <font color=\"comment\">${{ steps.wx_prep.outputs.wx_created }}</font> \n
          🔧 <font color=\"warning\">${{ steps.wx_prep.outputs.wx_base_ref || '∅' }} -> ${{ steps.wx_prep.outputs.wx_target_ref }}</font> \n
          📝 <font color=\"comment\"> ${{ github.event.head_commit.message }}</font> \n
          \n
          - [查看详情](https://www.hocgin.top)
          "
          mentioned_list: '["@all"]'

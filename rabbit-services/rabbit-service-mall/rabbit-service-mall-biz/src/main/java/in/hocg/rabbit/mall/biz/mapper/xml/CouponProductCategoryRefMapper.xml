<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="in.hocg.rabbit.mall.biz.mapper.CouponProductCategoryRefMapper">

    <select id="listProductCategoryByCouponId" resultType="in.hocg.rabbit.mall.biz.entity.ProductCategory">
      SELECT ppc.* FROM pms_product_category ppc
        LEFT JOIN mkt_coupon_product_category_ref mcpcr on ppc.id = mcpcr.product_category_id
      <where>
        AND mcpcr.coupon_id = #{couponId}
      </where>
    </select>
</mapper>

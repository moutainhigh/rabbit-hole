<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="in.hocg.rabbit.bmw.biz.mapper.AccessMchMapper">
  <select id="checkSupportPayType" resultType="java.lang.Integer">
    SELECT 1
    FROM bmw_pay_scene bps
           LEFT JOIN bmw_pay_scene_support bpss ON bps.id = bpss.pay_scene_id
    WHERE bps.access_mch_id = #{accessMchId}
      AND bpss.pay_type = #{payType}
    LIMIT 1
  </select>
</mapper>
